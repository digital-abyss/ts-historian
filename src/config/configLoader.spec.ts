import {loadConfig, Config} from './configLoader';

test('an empty filename throws an error', () => {
    
    
    //let res: Config = loadConfig('');
    //expect(res).toBe;

    //console.log(res);
});